name: demo
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Firebase (chỉ giữ phiên bản mới nhất)
  firebase_core: ^3.3.0
  firebase_auth: ^5.3.0
  firebase_database: ^11.1.0

  # AI & Machine Learning
  tflite_flutter: ^0.12.1

  # Image and camera utilities
  camera: ^0.10.5
  webview_flutter: ^4.4.2

  # Audio & Media
  audioplayers: ^5.2.1

  # Utilities
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/model.tflite
    - assets/sounds/